<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty
            scope="context"
            name="serviceName"
            source="spring.application.name"/>

    <appender name="JSON"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">

            <providers>

                <!-- Timestamp -->
                <timestamp>
                    <fieldName>timestamp</fieldName>
                </timestamp>

                <!-- Log Level -->
                <logLevel>
                    <fieldName>level</fieldName>
                </logLevel>

                <!-- Logger -->
                <loggerName>
                    <fieldName>logger</fieldName>
                </loggerName>

                <!-- Thread -->
                <threadName>
                    <fieldName>thread</fieldName>
                </threadName>

                <!-- Message -->
                <message/>

                <!-- traceId & spanId from OpenTelemetry/Micrometer -->
                <mdc/>

                <!-- Service Name -->
                <pattern>
                    <pattern>
                        {
                          "service":"${serviceName}"
                        }
                    </pattern>
                </pattern>

            </providers>

        </encoder>

    </appender>

    <root level="INFO">
        <appender-ref ref="JSON"/>
    </root>

</configuration>